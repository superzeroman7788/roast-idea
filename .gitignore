# --- Secrets (P7: 绝不进版本库) ---
.env
.env.local
.env.*.local
.env*.local
*.key
*.pem

# --- Dependencies / build ---
node_modules/
dist/
*.tsbuildinfo
.DS_Store

# --- Rust / Tauri ---
src-tauri/target/
src-tauri/gen/

# --- Local data (SQLite 落库，含原始点子，不进库) ---
*.db
*.db-shm
*.db-wal
data/

# --- 本地编辑器/预览配置(含机器绝对路径,不进库) ---
.claude/

# --- 启动动画的本地交接材料(app 实际用的副本在 public/launch/)---
*.zip
design_handoff_roast_idea_video/
